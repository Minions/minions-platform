/**
 * Wing Test Helpers - Re-exported from @minions/file-store
 *
 * This module re-exports test utilities from the file-store package.
 * The canonical implementation lives in @minions/file-store.
 */

// Re-export from file-store
export { createTestWing } from '@minions/file-store';
export type { CreateTestWingOptions } from '@minions/file-store';
